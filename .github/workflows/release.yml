name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write  # Required for creating releases

jobs:
  build-macos:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            arch: arm64
            name: Apple Silicon
          - target: x86_64-apple-darwin
            arch: x64
            name: Intel

    runs-on: macos-latest
    name: Build macOS (${{ matrix.name }})

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install dependencies
        run: npm ci

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0.5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Karaoke ${{ github.ref_name }}'
          releaseBody: |
            See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for details.

            ## Downloads
            - **Apple Silicon (M1/M2/M3)**: Download the `arm64` version
            - **Intel Macs**: Download the `x64` version
          releaseDraft: false
          prerelease: false
          args: --target ${{ matrix.target }}

  # Optional: Create universal binary (runs after both arch builds complete)
  # Uncomment if you want a single binary that works on both architectures
  # create-universal:
  #   needs: build-macos
  #   runs-on: macos-latest
  #   steps:
  #     - name: Download artifacts
  #       uses: actions/download-artifact@v4
  #     - name: Create universal binary
  #       run: |
  #         # Use lipo to create universal binary
  #         # lipo -create -output Karaoke-universal.app arm64/Karaoke.app x64/Karaoke.app
